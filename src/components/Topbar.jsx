import { Bell, Search, User } from "lucide-react";

export default function Topbar() {
  return (
    <div className="h-16 mb-8 flexitems-center justify-between">
      {/* Spacer to align with sticky sidebar */}
    </div>
  );
}
